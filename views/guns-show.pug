extends layout

block content
  .row
    .col-sm-4
      if gun.picUrlSq
        img(src=gun.picUrlSq).img-fluid
    .col-sm-8
      h1=gun.model
      p=gun.firingMode
      p=gun.caliber
      p=gun.description
      .text-right
        a(href="/guns/" + gun.id + "/edit").btn-link Edit
      
